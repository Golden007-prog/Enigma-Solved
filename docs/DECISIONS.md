# Decisions log

One line per decision: date · decision · why. Corrections to `docs/BLUEPRINT.md` / `docs/research/` are appended here, never edited in place.

- 2026-09-05 · Checkpoints (⛔) treated as pre-approved for this run · Oikantik's kickoff replaced "stop at every ⛔ until I say go" with "don't stop before completing the project". Every checkpoint action is kept reversible (FlutterFlow work on branch `interview-cracker`, migration saved as a file before apply, no admin/UAC changes performed by Claude) and is logged here.
- 2026-09-05 · Repo root `E:\Enigma for Masai` is the working tree of `github.com/Golden007-prog/Enigma-Solved` (branch `main`) · The FlutterFlow project and the user's screenshots point at that repo; the pre-existing `Enigma-Solved/` sub-clone (one commit, LICENSE only, identical remote) is now redundant. Claude was not permitted to delete it, so it is git-ignored; Oikantik can delete `E:\Enigma for Masai\Enigma-Solved` and the stray empty file `E:\Enigma for Masai\1.0` at any time.
- 2026-09-05 · Flutter SDK installed from the official zip to `C:\dev\flutter` (3.47.2 stable, Dart 3.13.2) · No winget package for Flutter exists (`winget search flutter` returns only unrelated store apps); path chosen has no spaces and is outside Program Files per Flutter's install guidance.
- 2026-09-05 · Android SDK via command-line tools + `sdkmanager`, not Android Studio · Android Studio is a multi-GB IDE the pipeline never uses; `sdkmanager` installs platform-tools, platform 35, build-tools, emulator and the x86_64 system image directly. `Google.AndroidStudio` remains available on winget if a GUI is ever wanted.
- 2026-09-05 · JDK 17 (`Microsoft.OpenJDK.17`) installed and bound to Flutter with `flutter config --jdk-dir`, leaving `JAVA_HOME` (Temurin JDK 25) untouched · Avoids changing a machine-wide variable other projects rely on; Flutter's Gradle wrapper needs a JDK it supports.
- 2026-09-05 · `C:\dev\flutter\bin` is prepended to the user PATH, so `dart` now resolves to Flutter's bundled Dart 3.13.2 (the standalone 3.13.3 at `C:\tools\dart-sdk` stays installed, later on PATH) · Keeps `flutter` and `dart` from the same SDK, which Flutter's tooling expects; `flutterflow_cli` 0.0.39 (activated under 3.13.3) still runs under 3.13.2 (`dart pub global list` verified). Revisit only if `flutterflow` complains about a snapshot version mismatch (fix: re-run `dart pub global activate flutterflow_cli`).
- 2026-09-05 · `flutterflow_cli` upgraded 0.0.38 → 0.0.39 · 0.0.39 adds `flutterflow ai projects --json` (non-interactive project listing) per the FlutterFlow build skill.
- 2026-09-05 · FlutterFlow project id = `enigma-solved-ctlkqt` (team "Oikantik Basu's Team", Growth plan) · Read from the project URL in Chrome; no key was needed to read it.
- 2026-09-05 · Master prompt Add-on A (Lottie micro-animations via LottieFiles MCP) acknowledged · Applies after Phase 4 starts; assets logged in `docs/ASSETS.md`. The LottieFiles MCP in this session exposes a GraphQL surface (`operations_list` / `graphql_execute`), so `searchPublicAnimations` / `publicAnimation` will be called through `graphql_execute`.
- 2026-09-05 · Android platform 36 + build-tools 36.0.0 installed in addition to the master prompt's platform 35 · Flutter 3.47.2's `flutter doctor` requires "Android SDK 36"; the android-35 system image is still the emulator target (Phase 6), so both platforms stay installed.
- 2026-09-05 · Visual Studio (C++ workload) deliberately not installed · Only needed for Flutter *Windows desktop* builds, which this project never targets; it is the sole red item in `flutter doctor`.
- 2026-09-05 · Qwen3.5-9B loaded **without** MTP speculative decoding · `lms load --speculative-draft-mtp` fails: the `lmstudio-community/Qwen3.5-9B-GGUF` Q6_K file has no bundled MTP head (the unsloth `Qwen3.5-9B-MTP-GGUF` build does, 7.68 GB). Measured 59 tok/s / 104 ms TTFT without it already beats the 35 tok/s / 400 ms targets, so the extra 7.7 GB download is deferred; revisit only if latency becomes the bottleneck.
- 2026-09-05 · Thinking is disabled per request (`reasoning_effort: "none"` + `chat_template_kwargs.enable_thinking=false`), verified by API (no `reasoning_content`, no `<think>` in 3/3 runs) · `lms load` cannot set the model's "Enable Thinking" field; the GUI toggle is optional belt-and-braces.
- 2026-09-05 · STT path = onnx-asr Parakeet-TDT-0.6B-v2 on CUDA with a **self-converted fp16 encoder** (`tools/convert_parakeet_fp16.py`, 1.22 GB) · onnx-asr exposes token-level emission timestamps (80 ms grid), not word spans, so word timestamps are derived by grouping tokens on the leading-space boundary. fp16 gave identical accuracy (99.2 vs 99.2 similarity), 235 vs 333 ms latency and ~1 GB less VRAM than fp32. The onnxconverter-common converter produced a mixed-type graph; onnxruntime's `OnnxModel.convert_float_to_float16` worked.
- 2026-09-05 · `onnxruntime-gpu` pinned to **1.24.4** (last CUDA-12 PyPI build) instead of 1.29 (CUDA 13) · torch 2.14+cu126 and ORT both load `cudnn64_9.dll`; mixing CUDA families in one process crashed Kokoro with "The specified procedure could not be found" (selftest). Same-family builds share torch's own CUDA DLLs.
- 2026-09-05 · Gate normalisation extended beyond the BLUEPRINT §5.1 sketch (NFKC + curly-quote/dash/NBSP fold, min 3 words) in `brain/rubric.py` · Pasted JDs carry U+2019/U+2013/U+2011/U+00A0 while a 9B model quotes ASCII; the plain sketch silently drops such quotes (proven by `fixtures/jd_fintech_unicode.txt`). Min-3-words stops "" / "you" / "Node.js" counting as provenance.
- 2026-09-05 · `hello` carries `jd`, `pressure`, `voice` (and later `device_id`); server adds a `rubric` event after Stage A · BLUEPRINT §4.1 names no message that moves the JD from LOBBY to the server, and the Prep page needs competency chips without questions.
- 2026-09-05 · Supabase schema deviates from the master prompt in three reviewed ways: (1) clients get SELECT+DELETE only on jds/sessions/turns/reports (server writes with the service role) so validated JSON cannot be forged from the phone; (2) `sessions.jd_id` is ON DELETE RESTRICT, not CASCADE; (3) `claim_guest_sessions` adopts only rows stamped with the device secret (the "JD referenced by a claimed session" sweep let one device adopt another's JD) and requires ≥ 32-char device ids · from the Phase 3 security review (workflow `phase3-supabase-draft`).
- 2026-09-05 · Advisor 0029 (SECURITY DEFINER callable by authenticated) on `claim_guest_sessions` is **accepted** · the rows it adopts match no RLS policy by design; it checks `auth.uid()`, only touches unclaimed rows, and needs the device secret.
- 2026-09-05 · Stage C (answer analysis) runs **off the critical path**: the server speaks the provisional next question as soon as Stage B words it, and Stage C's verdict/reaction/follow-up folds into the agenda while the candidate listens · measured 8–13 s answer-end→next-question with Stage C awaited (11 s of Qwen output); BLUEPRINT §5.3 says the candidate never waits for analysis. Cost: a follow-up demanded by Stage C arrives one question later, and the reaction mood lands ~10 s after the answer.
- 2026-09-05 · Gemini TTS is an **opt-in backend** (`TTS_BACKEND=gemini`), default remains Kokoro · Oikantik asked for it; it breaks the "local at runtime" non-negotiable when enabled, so it is off by default and logged loudly when on. Google documents `gemini-3.1-flash-tts-preview`, `gemini-2.5-pro-preview-tts`, `gemini-2.5-flash-preview-tts` (no "3.1 pro" TTS); default = 3.1 flash. Mouth track uses the §6.3 RMS fallback.
- 2026-09-05 · FlutterFlow app built as ONE greenfield DSL script (`app/ff-workspace/dsl/interview_cracker.dart`) run against the `interview-cracker` branch project id `1cEe3vhxwe7pRqSEeiKi` · `flutterflow ai` refuses `--project-id enigma-solved-ctlkqt` on a branch workspace (it names the branch's own project id instead). All cross-widget sharing goes through the `VoiceLinkHost` custom-widget file (`import '/custom_code/widgets/voice_link_host.dart'`) because FlutterFlow's `index.dart` re-exports use `show` clauses and the Code File import path is undocumented.
- 2026-09-05 · No Rive and no Lottie in the first app push · no `.riv` exists (CustomPaint avatar per Phase 5A) and the DSL has no asset-upload surface (Lottie widgets would need network URLs, which break offline). Add-on A is deferred to a follow-up edit once assets are uploaded in the FlutterFlow UI.
- 2026-09-05 · `path_provider` pinned `^2.1.5` via a dependency override · FlutterFlow's generated pubspec pins 2.1.4 while `flutter_soloud 3.5.4` requires ^2.1.5 (`flutter pub get` warning on the first push).
- 2026-09-05 · Stage B (provisional question) is requested **before** Stage C (analysis) is started · LM Studio runs `--parallel 1`, so requests are served strictly in arrival order; with Stage C fired first the question queued behind ~600 tokens of analysis and answer-end→tts_start was 9.5–12.5 s (e2e round 3). Reordered: 1.4–2.9 s (round 4 + 10-turn bench, p50 1.84 s / p95 2.86 s). Stage C still finishes while the question is being spoken.
- 2026-09-05 · Phone playback uses **flutter_pcm_sound 3.3.3** instead of flutter_soloud · every flutter_soloud 3.x requires `path_provider ^2.1.5`; FlutterFlow's generated pubspec pins `path_provider 2.1.4` and its code generator silently drops the `dependency_overrides` entry the SDK added (verified: override present in the project, absent from `generated_code/pubspec.yaml`), so `flutter pub get` failed on every export. flutter_pcm_sound has zero dependencies; the viseme clock is now "frames fed − frames still queued" (feed callback) with wall-clock extrapolation in between, instead of SoLoud's `getStreamTimeConsumed`.
- 2026-09-05 · Native config is re-applied by `app/tools/patch_native.py` after every `flutterflow export-code` (RECORD_AUDIO/CAMERA/MODIFY_AUDIO_SETTINGS, `usesCleartextTraffic`, minSdk 24, iOS usage strings + ATS local networking) · the CLI export overwrites the tree, so a `.flutterflowignore` would only help FlutterFlow's GitHub sync, which this project does not use.
- 2026-09-05 · Add-on A assets sourced through the LottieFiles **public GraphQL API** (`graphql.lottiefiles.com/2022-08`, no key) · the LottieFiles MCP server returned 404 ("Server not found") for the whole session; the GraphQL endpoint is the same surface the MCP wraps (`searchPublicAnimations`). Ten files, 4.8–57 KB each, recoloured to the palette by `app/tools/lottie_fetch.py`, logged in `docs/ASSETS.md`. The DSL has no asset-upload surface, so the JSON is embedded (gzip+base64) in the `StateCue` custom widget and rendered with `Lottie.memory` — works offline; switching to FlutterFlow's native Lottie widget needs a one-time asset upload in the UI.
- 2026-09-05 · Custom-code edits are verified with `flutter analyze` (and `flutter build web` when web publishing matters) on a **scratch copy** of `app/ff-workspace/generated_code` before the export is rebuilt · FlutterFlow's `run` only checks DSL shape, not Dart type correctness against the packages actually resolved; the published `flutter_pcm_sound 3.3.3` differs from its GitHub master (older `setup()`), which only the analyzer caught. Also: pub packages are read from the pub cache (`%LOCALAPPDATA%\Pub\Cache\hosted\pub.dev\<pkg>-<ver>`) rather than GitHub master when writing against them.
- 2026-09-05 · No `dart:io` in FlutterFlow custom code (StateCue gunzips with `package:archive`, pinned `archive 4.2.0` = what lottie 3.3.3 resolves) · FlutterFlow Web Publishing compiles with dart2js, which rejects `dart:io`; the second Claude session (enigma-for-masai-71) owns the web publish and found this. `voice_link_host.dart` has no `dart:io`.
- 2026-09-05 · Work split with a second Claude session (enigma-for-masai-71) · it owns `app/export`, `app/rc1_build`, the APK build/install, the emulator walk-through + screenshots, GitHub release/Pages, FlutterFlow Web Publishing, and PROGRESS Phase 4.5/6.2/7 lines under its own headings; this session owns `server/`, the DSL files (`app/ff-workspace/dsl`), `app/tools`, `app/assets/lottie`, the FlutterFlow pushes/merges, PROGRESS phases 2–3 + the Phase 4/5/6/7 lines it wrote, and DECISIONS entries for its own changes. Git: explicit paths only, message before push.
- 2026-09-05 · Local builds of the FlutterFlow export should use **Flutter 3.38.5** (FlutterFlow's own toolchain), not the machine's 3.47.2 · 3.47.2 rejects the template's pinned `font_awesome_flutter 10.7.0` (`IconData` is now `final`) and `page_transition 2.1.0`, and needs the intl/Gradle 8.14.3/AGP 8.11.1 rewrites in `patch_native.py`. The rewrites stay (they are idempotent and harmless) but the recommended path is a side-by-side 3.38.5 SDK. Phase 0's 3.47.2 install remains for `flutter doctor` / tooling.
- 2026-09-05 · The FlutterFlow branch → main merge was **not executed by Claude** · the auto-mode permission classifier refused `flutterflow ai merge start` in this session; per the cross-session rules the peer session was not asked to run it either. It is Oikantik's call (CLI merge loop or FlutterFlow UI); the branch head to merge is `in6N3VtqTcKn8ALKDye6`.
- 2026-09-05 · `*.wav` git-ignored except `server/fixtures/*.wav` · Master prompt Part D ignores `*.wav` but Phase 1 requires committed fixture audio for tests; the exception keeps the small scripted fixtures in git and every recorded session out.
